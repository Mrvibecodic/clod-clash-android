diff --git a/adapter/outbound/anytls.go b/adapter/outbound/anytls.go
index 99786614..84d30efe 100644
--- a/adapter/outbound/anytls.go
+++ b/adapter/outbound/anytls.go
@@ -86,6 +86,12 @@ func (t *AnyTLS) ProxyInfo() C.ProxyInfo {
 	return info
 }
 
+func (t *AnyTLS) ResetNetwork() {
+	if t.client != nil {
+		t.client.ResetNetwork()
+	}
+}
+
 // Close implements C.ProxyAdapter
 func (t *AnyTLS) Close() error {
 	return t.client.Close()
diff --git a/adapter/outbound/base.go b/adapter/outbound/base.go
index 01e00798..d55930b6 100644
--- a/adapter/outbound/base.go
+++ b/adapter/outbound/base.go
@@ -381,6 +381,12 @@ func (p *autoCloseProxyAdapter) ListenPacketContext(ctx context.Context, metadat
 	return pc, nil
 }
 
+func (p *autoCloseProxyAdapter) ResetNetwork() {
+	if r, ok := p.ProxyAdapter.(interface{ ResetNetwork() }); ok {
+		r.ResetNetwork()
+	}
+}
+
 func (p *autoCloseProxyAdapter) Close() error {
 	p.closeOnce.Do(func() {
 		log.Debugln("Closing outdated proxy [%s]", p.Name())
diff --git a/adapter/outbound/hysteria2.go b/adapter/outbound/hysteria2.go
index 4731611f..81f06617 100644
--- a/adapter/outbound/hysteria2.go
+++ b/adapter/outbound/hysteria2.go
@@ -111,6 +111,12 @@ func (h *Hysteria2) ListenPacketContext(ctx context.Context, metadata *C.Metadat
 	return NewPacketConn(N.NewThreadSafePacketConn(pc), h), nil
 }
 
+func (h *Hysteria2) ResetNetwork() {
+	if h.client != nil {
+		_ = h.client.CloseWithError(errors.New("network changed"))
+	}
+}
+
 // Close implements C.ProxyAdapter
 func (h *Hysteria2) Close() error {
 	if h.client != nil {
diff --git a/adapter/outbound/singmux.go b/adapter/outbound/singmux.go
index 57a64c2e..e1ab84a0 100644
--- a/adapter/outbound/singmux.go
+++ b/adapter/outbound/singmux.go
@@ -83,6 +83,15 @@ func (s *SingMux) ProxyInfo() C.ProxyInfo {
 	return info
 }
 
+func (s *SingMux) ResetNetwork() {
+	if s.client != nil {
+		s.client.Reset()
+	}
+	if r, ok := s.ProxyAdapter.(interface{ ResetNetwork() }); ok {
+		r.ResetNetwork()
+	}
+}
+
 // Close implements C.ProxyAdapter
 func (s *SingMux) Close() error {
 	if s.client != nil {
diff --git a/adapter/outbound/ssh.go b/adapter/outbound/ssh.go
index 601dfc84..ce52c43b 100644
--- a/adapter/outbound/ssh.go
+++ b/adapter/outbound/ssh.go
@@ -102,6 +102,15 @@ func (s *Ssh) ProxyInfo() C.ProxyInfo {
 	return info
 }
 
+func (s *Ssh) ResetNetwork() {
+	s.cMutex.Lock()
+	defer s.cMutex.Unlock()
+	if s.client != nil {
+		_ = s.client.Close()
+		s.client = nil
+	}
+}
+
 // Close implements C.ProxyAdapter
 func (s *Ssh) Close() error {
 	s.cMutex.Lock()
diff --git a/adapter/outbound/trojan.go b/adapter/outbound/trojan.go
index d581e80a..ddcc5e81 100644
--- a/adapter/outbound/trojan.go
+++ b/adapter/outbound/trojan.go
@@ -269,6 +269,12 @@ func (t *Trojan) ProxyInfo() C.ProxyInfo {
 	return info
 }
 
+func (t *Trojan) ResetNetwork() {
+	if t.gunClient != nil {
+		_ = t.gunClient.Close()
+	}
+}
+
 // Close implements C.ProxyAdapter
 func (t *Trojan) Close() error {
 	var errs []error
diff --git a/adapter/outbound/tuic.go b/adapter/outbound/tuic.go
index ed54d99f..40ab12ce 100644
--- a/adapter/outbound/tuic.go
+++ b/adapter/outbound/tuic.go
@@ -70,6 +70,12 @@ type TuicOption struct {
 }
 
 // DialContext implements C.ProxyAdapter
+func (t *Tuic) ResetNetwork() {
+	if t.client != nil {
+		t.client.ResetNetwork()
+	}
+}
+
 func (t *Tuic) DialContext(ctx context.Context, metadata *C.Metadata) (C.Conn, error) {
 	conn, err := t.client.DialContext(ctx, metadata)
 	if err != nil {
diff --git a/adapter/outbound/vless.go b/adapter/outbound/vless.go
index 6b9edd38..93745913 100644
--- a/adapter/outbound/vless.go
+++ b/adapter/outbound/vless.go
@@ -414,6 +414,12 @@ func (v *Vless) ProxyInfo() C.ProxyInfo {
 	return info
 }
 
+func (v *Vless) ResetNetwork() {
+	if v.gunClient != nil {
+		_ = v.gunClient.Close()
+	}
+}
+
 // Close implements C.ProxyAdapter
 func (v *Vless) Close() error {
 	var errs []error
diff --git a/adapter/outbound/vmess.go b/adapter/outbound/vmess.go
index aef7cce8..ed9b3a52 100644
--- a/adapter/outbound/vmess.go
+++ b/adapter/outbound/vmess.go
@@ -445,6 +445,12 @@ func (v *Vmess) ProxyInfo() C.ProxyInfo {
 	return info
 }
 
+func (v *Vmess) ResetNetwork() {
+	if v.gunClient != nil {
+		_ = v.gunClient.Close()
+	}
+}
+
 // Close implements C.ProxyAdapter
 func (v *Vmess) Close() error {
 	var errs []error
diff --git a/transport/anytls/client.go b/transport/anytls/client.go
index fce4d4a6..c3601d61 100644
--- a/transport/anytls/client.go
+++ b/transport/anytls/client.go
@@ -98,6 +98,10 @@ func (c *Client) createOutboundTLSConnection(ctx context.Context) (net.Conn, err
 	return tlsConn, nil
 }
 
+func (h *Client) ResetNetwork() {
+	h.sessionClient.ResetSessions()
+}
+
 func (h *Client) Close() error {
 	return h.sessionClient.Close()
 }
diff --git a/transport/anytls/session/client.go b/transport/anytls/session/client.go
index 2e038557..51143d2e 100644
--- a/transport/anytls/session/client.go
+++ b/transport/anytls/session/client.go
@@ -150,6 +150,29 @@ func (c *Client) createSession(ctx context.Context) (*Session, error) {
 	return session, nil
 }
 
+func (c *Client) ResetSessions() {
+	c.idleSessionLock.Lock()
+	it := c.idleSession.Iterate()
+	for it.IsNotEnd() {
+		key := it.Key()
+		it.MoveToNext()
+		c.idleSession.Remove(key)
+	}
+	c.idleSessionLock.Unlock()
+
+	c.sessionsLock.Lock()
+	sessionToClose := make([]*Session, 0, len(c.sessions))
+	for _, session := range c.sessions {
+		sessionToClose = append(sessionToClose, session)
+	}
+	c.sessions = make(map[uint64]*Session)
+	c.sessionsLock.Unlock()
+
+	for _, session := range sessionToClose {
+		session.Close()
+	}
+}
+
 func (c *Client) Close() error {
 	c.dieCancel()
 
diff --git a/transport/tuic/pool_client.go b/transport/tuic/pool_client.go
index 82efbfe1..59f267a5 100644
--- a/transport/tuic/pool_client.go
+++ b/transport/tuic/pool_client.go
@@ -119,6 +119,21 @@ func (t *PoolClient) getClient(udp bool) Client {
 	}
 }
 
+func (t *PoolClient) ResetNetwork() {
+	closeAll := func(clients *list.List[Client], clientsMutex *sync.Mutex) {
+		clientsMutex.Lock()
+		defer clientsMutex.Unlock()
+		for it := clients.Front(); it != nil; it = it.Next() {
+			if it.Value != nil {
+				it.Value.Close()
+			}
+		}
+		clients.Init()
+	}
+	closeAll(&t.tcpClients, &t.tcpClientsMutex)
+	closeAll(&t.udpClients, &t.udpClientsMutex)
+}
+
 func NewPoolClientV4(clientOption *ClientOptionV4, dialFn DialFunc) *PoolClient {
 	p := &PoolClient{
 		dialFn: dialFn,
